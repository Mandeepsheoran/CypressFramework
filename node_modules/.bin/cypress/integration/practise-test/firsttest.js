const cypress = require("cypress");
const { describe } = require("mocha");

describe('DT access', function() {

it('DT login', function() {
    cypress.visit("https://dev-ui.azure.digitaltwin.aero/login")
    cy.reload();
    window.location.reload()
    cy.hash()
    window.location.hash()
    cy.go(-1)
    window.history.back()
    cy.go(1)
    window.history.forward()
    cy.clearLocalStorage()
    window.localStorage()
    window.sessionStorage()
    
}

)


}
)